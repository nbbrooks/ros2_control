# The ConfigureHardwareComponent service allows you to configure a single hardware component
# inside resource manager

# To configure a hardware component, specify the "name" of the component / hardware interface.
# The return value "ok" indicates if the component was successfully configured or not.

string name
---
bool ok
