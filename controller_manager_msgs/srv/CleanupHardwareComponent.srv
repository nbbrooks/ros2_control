# The CleanupHardwareComponent service allows you to cleanup a single hardware component
# inside resource manager.

# To cleanup a hardware component, specify the "name" of the component / hardware interface.
# The return value "ok" indicates if the component was successfully cleaned-up or not.

string name
---
bool ok
